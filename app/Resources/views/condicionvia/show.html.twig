{% extends 'base.html.twig' %}

{% block body %}
    <h1>Condicion de la Vía</h1>

    <table>
        <tbody>
            <tr>
                <th>Descripcion</th>
                <td>{{ condicionVium.descripcion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ condicionVium.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('condicionvia_index') }}">Volver a la lista</a>
        </li>
        <li>
            <a href="{{ path('condicionvia_edit', { 'id': condicionVium.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
