{% extends 'base.html.twig' %}

{% block body %}
    <h1>CondicionVia</h1>

    <table>
        <tbody>
            <tr>
                <th>Descripcion</th>
                <td>{{ condicionVium.descripcion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ condicionVium.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('condicionvia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('condicionvia_edit', { 'id': condicionVium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
