{% extends 'base.html.twig' %}

{% block body %}
    <h1>CondicionVia list</h1>

    <table>
        <thead>
            <tr>
                <th>Descripcion</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for condicionVium in condicionVias %}
            <tr>
                <td><a href="{{ path('condicionvia_show', { 'id': condicionVium.id }) }}">{{ condicionVium.descripcion }}</a></td>
                <td>{{ condicionVium.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('condicionvia_show', { 'id': condicionVium.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('condicionvia_edit', { 'id': condicionVium.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('condicionvia_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
